````md
## 📌 Reflow vs Repaint

브라우저 렌더링 과정에서 **성능에 큰 영향을 주는 단계**가 바로 **Reflow(Layout)**와 **Repaint(Paint)** 입니다.  
DOM이나 스타일이 변경되면 브라우저는 화면을 다시 계산하고 그려야 합니다.

---

## 1️⃣ Reflow (Layout)

### ✔ 개념

**Reflow**는 **요소의 크기나 위치가 변경되어 Layout을 다시 계산하는 과정**입니다.  
브라우저가 요소의 **위치, 크기, 구조를 다시 계산**해야 하므로 비용이 큽니다.

> Layout 재계산 → Render Tree 재구성 → Paint → Composite

### ✔ 특징

- 성능 비용이 큼
- 부모 → 자식 → 형제 요소까지 영향 가능
- DOM 구조 변경 시 발생

### ✔ 발생 예시

```js
element.style.width = "200px";
element.style.height = "100px";
element.style.margin = "10px";
```
````

```js
element.appendChild(newDiv);
```

```js
element.remove();
```

### ✔ 주요 Reflow 발생 요소

- width / height 변경
- margin / padding 변경
- border 변경
- font-size 변경
- display 변경
- position 변경
- DOM 추가/삭제
- window resize
- offsetWidth 조회

```js
element.offsetWidth; // Reflow 강제 발생 가능
```

### ✔ 문제점

- CPU 사용량 증가
- 애니메이션 끊김
- 스크롤 버벅임

---

## 2️⃣ Repaint (Paint)

### ✔ 개념

**Repaint**는 **요소의 위치나 크기는 그대로이고 스타일만 변경될 때 발생**합니다.

Layout 계산은 하지 않고 **다시 그리기만 수행**합니다.

> Paint → Composite

---

### ✔ 특징

- Reflow보다 비용이 낮음
- Layout 계산 없음

---

### ✔ 발생 예시

```js
element.style.color = "red";
element.style.backgroundColor = "blue";
element.style.visibility = "hidden";
```

---

### ✔ 주요 Repaint 발생 요소

- color 변경
- background 변경
- visibility 변경
- outline 변경

---

## 3️⃣ Reflow vs Repaint 비교

| 구분        | Reflow        | Repaint     |
| ----------- | ------------- | ----------- |
| 의미        | Layout 재계산 | 다시 그리기 |
| 비용        | 높음          | 낮음        |
| Layout 계산 | O             | X           |
| Paint 발생  | O             | O           |
| Composite   | O             | O           |

---

## 4️⃣ 포함 관계

```
Reflow ⊃ Repaint
```

즉,

- Reflow 발생 시 → Repaint 발생
- Repaint 발생 시 → Reflow는 발생하지 않음

---

## 5️⃣ 성능 최적화 방법

### ✔ 1. DOM 변경 최소화

❌ 나쁜 예

```js
for (let i = 0; i < 100; i++) {
  list.appendChild(document.createElement("div"));
}
```

✔ 좋은 예

```js
const fragment = document.createDocumentFragment();

for (let i = 0; i < 100; i++) {
  fragment.appendChild(document.createElement("div"));
}

list.appendChild(fragment);
```

---

### ✔ 2. 스타일 변경 묶기

❌ 나쁜 예

```js
element.style.width = "100px";
element.style.height = "200px";
element.style.margin = "10px";
```

✔ 좋은 예

```js
element.style.cssText = `
  width:100px;
  height:200px;
  margin:10px;
`;
```

---

### ✔ 3. class 변경 사용

❌ 나쁜 예

```js
element.style.color = "red";
element.style.background = "blue";
```

✔ 좋은 예

```js
element.classList.add("active");
```

---

### ✔ 4. 애니메이션은 transform 사용

❌ 나쁜 예 (Reflow 발생)

```css
left: 100px;
top: 100px;
width: 200px;
```

✔ 좋은 예 (Composite 단계)

```css
transform: translateX(100px);
```

---

### ✔ 5. display 대신 visibility 사용

❌ 나쁜 예 (Reflow)

```css
display: none;
```

✔ 좋은 예 (Repaint)

```css
visibility: hidden;
```

---

## 6️⃣ 강제 Reflow (Layout Thrashing)

Layout 값을 조회하면 브라우저가 강제로 Reflow를 수행할 수 있습니다.

### ✔ 발생 예

```js
element.style.width = "200px";

element.offsetWidth; // 강제 Reflow 발생

element.style.width = "300px";
```

---

### ✔ 해결 방법

읽기와 쓰기를 분리합니다.

❌ 나쁜 예

```js
element.style.width = "200px";
element.offsetWidth;
element.style.width = "300px";
```

✔ 좋은 예

```js
const width = element.offsetWidth;

element.style.width = width + 100 + "px";
```

---

## 7️⃣ 렌더링 파이프라인과 관계

브라우저 렌더링 과정:

1. DOM 생성
2. CSSOM 생성
3. Render Tree 생성
4. Layout (Reflow)
5. Paint (Repaint)
6. Composite

---

## ✅ 핵심 정리

### ✔ Reflow

- Layout 재계산
- 비용 높음
- 성능 영향 큼

### ✔ Repaint

- 색상/스타일 변경
- 비용 낮음
- Layout 없음

---

## ✅ 한 줄 정리

> **성능 최적화 핵심은 Reflow를 줄이는 것이다.**
