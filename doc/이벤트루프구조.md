---

````md
# 📌 JavaScript Event Loop 이론 정리

## 1. Event Loop란?

Event Loop는 JavaScript의 **비동기 처리 메커니즘**이다.  
JavaScript는 기본적으로 **Single Thread** 기반으로 동작하지만,  
Event Loop 덕분에 비동기 처리가 가능하다.

JS 엔진은 대표적으로 **V8 엔진 (Chrome, Node.js)** 에서 실행된다.

---

## 2. Event Loop 구조

Event Loop는 다음과 같은 구성 요소로 이루어진다.

- Call Stack
- Web APIs
- Task Queue (Macrotask Queue)
- Microtask Queue

---

## 3. 각 구성 요소 설명

### 📍 1) Call Stack

- 실행 중인 함수가 쌓이는 영역
- LIFO (Last In First Out) 구조
- 함수 실행 → 스택에 push
- 실행 종료 → 스택에서 pop

```js
function a() {
  b();
}
function b() {
  console.log("Hello");
}
a();
````

실행 순서:

1. `a` push
2. `b` push
3. `console.log` 실행
4. `b` pop
5. `a` pop

---

### 📍 2) Web APIs (브라우저 환경)

* 브라우저가 제공하는 비동기 API
* JS 엔진 외부에서 동작

예시:

* setTimeout
* setInterval
* fetch
* DOM Events

```js
setTimeout(() => {
  console.log("Timer");
}, 1000);
```

`setTimeout`은 Call Stack이 아니라 Web APIs에서 타이머를 처리한다.

---

### 📍 3) Task Queue (Macrotask Queue)

* 일반적인 비동기 콜백이 들어가는 큐
* FIFO (First In First Out)

대표 예시:

* setTimeout
* setInterval
* I/O
* UI Rendering

---

### 📍 4) Microtask Queue

* Macrotask보다 **우선순위가 높음**
* 현재 작업이 끝난 직후 즉시 실행

대표 예시:

* Promise.then
* Promise.catch
* Promise.finally
* MutationObserver
* queueMicrotask

```js
Promise.resolve().then(() => {
  console.log("Microtask");
});
```

---

## 4. Event Loop 실행 순서

Event Loop는 아래 순서로 동작한다:

1. Call Stack이 완전히 비워질 때까지 실행
2. Microtask Queue에 있는 작업을 **모두 실행**
3. Macrotask Queue에서 **1개만 실행**
4. 다시 1번으로 돌아가 반복

---

## 5. 실행 예제 분석

```js
console.log("Start");

setTimeout(() => {
  console.log("Macrotask");
}, 0);

Promise.resolve().then(() => {
  console.log("Microtask");
});

console.log("End");
```

### 실행 순서 분석

1. Start 출력
2. End 출력
3. Microtask 실행
4. Macrotask 실행

### 최종 출력

```
Start
End
Microtask
Macrotask
```

---

## 6. 정리

| 구분              | 우선순위  | 예시           |
| --------------- | ----- | ------------ |
| Call Stack      | 가장 높음 | 일반 함수        |
| Microtask Queue | 높음    | Promise.then |
| Macrotask Queue | 낮음    | setTimeout   |

✔ 핵심 포인트:

* Microtask는 Macrotask보다 항상 먼저 실행된다.
* Microtask는 한 번 실행되면 큐가 빌 때까지 모두 실행된다.
* JavaScript는 Single Thread이지만 Event Loop 덕분에 비동기 처리가 가능하다.

---

## 7. 한 줄 요약

> "Call Stack이 비면 → Microtask 전부 실행 → Macrotask 하나 실행 → 반복"

