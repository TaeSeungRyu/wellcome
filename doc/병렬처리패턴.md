---

# 📌 Promise 병렬 처리 패턴 정리

JavaScript에서 여러 비동기 작업을 **동시에 실행(병렬 처리)** 할 때
주로 사용하는 Promise API 패턴을 정리합니다.

---

## 1️⃣ `Promise.all`

### ✅ 특징

* 모든 Promise가 **성공(resolve)** 해야 성공
* 하나라도 **실패(reject)** 하면 즉시 실패
* 가장 많이 사용하는 병렬 처리 방식
* 결과는 **입력 순서 그대로 배열로 반환**

---

### 📌 기본 문법

```js
Promise.all([promise1, promise2, promise3])
  .then(results => {
    console.log(results);
  })
  .catch(error => {
    console.error(error);
  });
```

---

### 📌 예제

```js
const p1 = Promise.resolve("A");
const p2 = Promise.resolve("B");
const p3 = Promise.resolve("C");

Promise.all([p1, p2, p3]).then(console.log);
```

출력:

```
["A", "B", "C"]
```

---

### ❗ 하나라도 실패하면

```js
const p1 = Promise.resolve("A");
const p2 = Promise.reject("Error");
const p3 = Promise.resolve("C");

Promise.all([p1, p2, p3])
  .then(console.log)
  .catch(console.error);
```

출력:

```
Error
```

👉 즉시 reject (다른 Promise는 기다리지 않음)

---

### 💡 사용 사례

* 여러 API를 동시에 요청
* 여러 파일 업로드
* 초기 데이터 로딩

---

## 2️⃣ `Promise.race`

### ✅ 특징

* 가장 **먼저 완료된 Promise** 결과 반환
* 성공이든 실패든 상관없이 가장 빠른 결과

---

### 📌 기본 문법

```js
Promise.race([promise1, promise2])
  .then(result => console.log(result))
  .catch(error => console.error(error));
```

---

### 📌 예제

```js
const fast = new Promise(resolve =>
  setTimeout(() => resolve("fast"), 100)
);

const slow = new Promise(resolve =>
  setTimeout(() => resolve("slow"), 200)
);

Promise.race([fast, slow]).then(console.log);
```

출력:

```
fast
```

---

### ❗ 실패가 더 빠르면?

```js
const success = new Promise(resolve =>
  setTimeout(() => resolve("ok"), 200)
);

const fail = new Promise((_, reject) =>
  setTimeout(() => reject("error"), 100)
);

Promise.race([success, fail])
  .then(console.log)
  .catch(console.error);
```

출력:

```
error
```

---

### 💡 사용 사례

* 요청 타임아웃 처리
* 가장 빠른 서버 선택
* fallback 전략

---

## 3️⃣ `Promise.allSettled`

### ✅ 특징

* 모든 Promise가 끝날 때까지 기다림
* 성공/실패 여부와 관계없이 **모든 결과 반환**
* 절대 reject하지 않음

---

### 📌 기본 문법

```js
Promise.allSettled([promise1, promise2])
  .then(results => console.log(results));
```

---

### 📌 예제

```js
const p1 = Promise.resolve("A");
const p2 = Promise.reject("Error");

Promise.allSettled([p1, p2]).then(console.log);
```

출력:

```js
[
  { status: "fulfilled", value: "A" },
  { status: "rejected", reason: "Error" }
]
```

---

### 💡 사용 사례

* 여러 API 요청 중 일부 실패 허용
* 배치 작업 처리
* 통계/로그 수집

---

## 4️⃣ 비교 정리

| 메서드                  | 성공 조건    | 실패 처리               | 반환 형태    |
| -------------------- | -------- | ------------------- | -------- |
| `Promise.all`        | 모두 성공    | 하나라도 실패 시 즉시 reject | 성공 값 배열  |
| `Promise.race`       | 가장 먼저 완료 | 가장 먼저 실패하면 reject   | 단일 값     |
| `Promise.allSettled` | 항상 성공    | 없음 (status로 구분)     | 상태 객체 배열 |

---

## 5️⃣ async/await과 함께 사용

### `Promise.all` + await

```js
async function fetchAll() {
  const [user, posts] = await Promise.all([
    fetchUser(),
    fetchPosts()
  ]);

  console.log(user, posts);
}
```

👉 병렬 처리 (순차 아님)

---

### ❌ 잘못된 순차 처리 예

```js
const user = await fetchUser();
const posts = await fetchPosts();
```

👉 위 코드는 병렬이 아니라 **순차 실행**

---

## 6️⃣ 병렬 vs 순차 차이

### 🔹 순차 실행

```js
await A();
await B();
```

총 시간 = A + B

---

### 🔹 병렬 실행

```js
await Promise.all([A(), B()]);
```

총 시간 = max(A, B)

---

## 7️⃣ 실무 팁

### 🔥 API 3개 이상이면 대부분 `Promise.all`

### 🔥 실패 허용이면 `Promise.allSettled`

### 🔥 타임아웃 구현은 `Promise.race`

```js
function timeout(ms) {
  return new Promise((_, reject) =>
    setTimeout(() => reject("Timeout"), ms)
  );
}

await Promise.race([
  fetchData(),
  timeout(3000)
]);
```

---

## 📌 한 줄 요약

* `Promise.all` → 전부 성공해야 성공
* `Promise.race` → 가장 빠른 결과
* `Promise.allSettled` → 성공/실패 모두 반환

---