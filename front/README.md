## ⚙️ 기술 스택

- **React 19**
- **Vite 7**
- **TypeScript**
- **TanStack Router**
- **TanStack Devtools**
- **TanStack Query**
- **React hook form**
- **zod**
- **zustand**
- **Vitest / Testing Library**

---

## 📁 프로젝트 구조(요약)

```
src/
 ├─ routes/           # TanStack Router file-based routing
 │   ├─ __root.tsx    # 루트 라우트
 │   └─ domain/       # 도메인별 디렉토리(예 : home, login)
 │        ├─ route.tsx     # 레이아웃 파일
 │        └─ domain/       # 페이지 디렉토리
 │             ├─ _/        # 서비스 및 hook 계층 디렉토리
 │             │  ├─ **Hook.tsx          # 해당 도메인의 hook 파일
 │             │  └─ **Repository.ts     # 해당 도메인의 서비스(비즈니스 로직) 파일
 │             └─ route.tsx     # 페이지
 ├─ components/  # 공용 컴포넌트
 └─ main.tsx  # 메인 파일
```

---

## 🚀 주요 기능

### 1) **파일 기반 라우팅**

- `src/routes` 디렉토리 안의 파일 이름이 자동으로 URL로 매핑됨
- `__root.tsx` → 최상위 루트

### 2) **레이아웃 기능**

- `<Outlet />`을 통해 페이지 콘텐츠 렌더링
- 헤더/푸터 공통 레이아웃 가능

### 3) **Vite 기반 고속 개발 서버**

- `vite --port 3000` 실행
- Hot Module Reloading 지원

### 4) **TypeScript 적용**

- 타입 안정성 및 컴파일 오류 방지

### 5) **Devtools 지원**

- TanStack Router Devtools
- React Devtools
- 개발 시 라우트 상태와 네비게이션 확인 가능

### 6) **테스트 환경 포함**

- Vitest
- React Testing Library
- DOM 테스트를 위한 jsdom 환경 포함

---

## 🧩 Scripts

| 명령어            | 설명                       |
| ----------------- | -------------------------- |
| `npm run dev`     | 개발 서버 실행 (3000 포트) |
| `npm run build`   | 프로덕션 빌드 + 타입 체크  |
| `npm run preview` | 빌드 후 로컬 프리뷰        |
| `npm run test`    | Vitest 테스트 실행         |

---

## 📦 설치 & 실행

```bash
npm install
npm run dev
```

---
