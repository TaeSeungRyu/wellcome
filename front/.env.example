# 클라이언트가 호출할 API base URL
# - 같은 origin의 nginx/vite 프록시 사용 시: /api
# - 외부 도메인 직접 호출 시: https://api.example.com
VITE_API_BASE_URL=/api

# (개발 전용) Vite dev 서버가 /api 요청을 포워딩할 백엔드 주소
# .env.development에 두면 vite dev 시에만 자동 적용됨
VITE_API_PROXY_TARGET=http://localhost:8080
